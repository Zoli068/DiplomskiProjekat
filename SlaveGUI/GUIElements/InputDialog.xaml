<Window x:Class="SlaveGUI.GUIElements.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SlaveGUI.GUIElements"
        mc:Ignorable="d"
                Title="Enter value" Height="130" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock FontSize="14" Grid.Row="0" Grid.Column="0" Margin="0,8,5,5">Value:</TextBlock>
        <TextBox FontSize="14" Grid.Row="0" Grid.Column="1" x:Name="InputBox" Margin="0,5,5,5"/>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Right" >
            <Button FontSize="14" Content="OK" Width="70" Click="Ok_Click" Margin="0,0,5,5"/>
            <Button FontSize="14" Content="Cancel" Width="70" Click="Cancel_Click" Margin="0,0,5,5"/>
        </StackPanel>
    </Grid>
</Window>